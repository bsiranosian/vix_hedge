# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/

# Cleaned parquet caches are built from the (licensed, not-redistributable)
# OptionMetrics data in ../option_data -- never commit them. Rebuild with
#   uv run python -m vix_hedge.data.build
# See the README "Data" section.
/data/cache/

# Result CSVs are regenerated by the run_*.py scripts; the committed deliverables
# are the interactive HTML reports + PNG charts under results/.
results/**/*.csv

# OS / editor cruft
.DS_Store
*.swp
.*.swp
*.swo
*~
